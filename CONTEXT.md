# 项目统一术语表

本文档定义 CAD 图纸 3D 建模系统中领域术语的精确含义。不含实现细节。

---

## 处理模式

| 术语 | 含义 |
|------|------|
| 智能处理 | 面向用户的统一处理入口。GUI 中不再暴露基础/智能二选一；所有图纸都先经过大模型参与的图纸理解，再由系统选择后续建模路径。 |
| 平面拉伸路径 | 智能处理内部的一条低复杂度建模路径；当图纸已被判定为可平面拉伸图时，执行直接轮廓拉伸，不承担图纸理解职责。 |
| 专用建模路径 | 面向某一类稳定建模家族的确定性执行路径，例如平面拉伸路径；后续可继续扩展回转体路径等。 |
| 回转体路径 | 面向轴对称回转件的专用建模路径；当前仅支持已明确给出轴线和闭合母线点列的受约束回转体。 |
| 语义重建路径 | 智能处理内部的兜底建模路径；当图纸无法稳定归入某条专用建模路径时，用于承接需要三维语义推理和复杂特征重建的情况。 |
| 分析缓存 | `.cache/analysis` 下的智能分析结果缓存，按文件+参数幂等；只缓存完整的、可直接继续执行的分析结果，不含未决澄清问题的半成品。 |

## 分析流水线

| 术语 | 含义 |
|------|------|
| CAD 解析 | 将 DXF/DWG 文件转换为标准化 `geometry_data` 字典。 |
| 智能分析 | 智能处理编排中的分析子过程，负责视图识别、尺寸提取、LLM 视图校正、本地关系分析和语义结果生成。 |
| 智能处理编排 | 智能模式中的主流程组织层；负责串联分析子过程、语义裁决、建模路径选择和后续处理分发。 |
| GUI 批量处理 | GUI 中对 CAD 文件列表里用户选中的多张图纸按队列执行智能处理的交互流程；它不是默认处理整个输入目录，也不应绕过用户对待处理图纸集合的选择。 |
| 批量进度窗口 | GUI 批量处理开始时自动打开的非模态任务看板；按图纸展示队列状态、阶段映射进度条、当前阶段文本和处理结果，批量结束后保留最终状态供用户查看，用户可手动关闭并在主界面重新打开。进度条在当前阶段区间内随时间缓慢推进，但不代表真实 ETA；窗口只提供展示和打开输出目录，不替代主界面的暂停、取消、日志、大模型调用或待恢复任务列表。 |
| 待恢复任务 | GUI 中保存现场、等待用户恢复的单张图纸任务；可来自 `needs_clarification`，也可来自已生成主体模型但存在跳过细节的 `partial_completed`。系统必须保存追问清单、澄清上下文、输入文件、输出目录、处理参数和已生成产物信息，使用户之后可以从该图纸的恢复点补充信息并重新生成模型，而批量队列可先处理下一张图纸。 |
| 建模执行分发 | 智能处理编排之后的执行分发层；消费建模路径裁决，把任务转交给平面拉伸路径、回转体路径或 AI 脚本运行器，并回填处理结果状态与产物路径。 |
| 语义重建内核 | 智能处理编排内部调用的深层模块；负责语义裁决、零件语义生成、建模路径选择以及建模指令或平面拉伸路由产出。 |
| 语义重建结果装配器 | 语义重建内核中统一装配对外返回结构的角色；负责组合重建上下文、语义策略、零件语义、建模路径裁决、建模结果和必要的路径层追问上下文。它不执行阶段流程、不生成建模脚本，也不应用用户答案。 |
| 智能分析结果 | 智能分析子过程形成的结构化产物，包括视图结果、尺寸提取、本地关系、语义结果及其证据。 |
| 智能处理结果 | 智能模式任务的外层结果；包含智能分析结果、建模路径、执行分支、最终产物与处理状态。 |
| 部分建模成果 | 智能处理已生成可打开、可检查、可导出的模型主体，且主外形、主要体量、方向和关键尺寸来源没有明显错误；孔、倒角、圆角、槽、螺纹、局部切除等细节可因语义不足、低置信特征或局部建模失败被跳过。主体可建时，细节不确定不应阻塞整体建模；不确定的是主体外形、主要体量、方向或关键尺寸来源时，才进入 `needs_clarification`。它不是普通失败，也不等同于完整完成，必须显式列出缺失内容和风险。 |
| 跳过细节 | 部分建模成果中的未完成细节项；通常是孔、倒角、圆角、槽、螺纹或局部切除等不会改变主体外形和主要体量的特征。跳过细节必须结构化记录原因，不能只混入普通 warning 文本。 |
| 视图分析 | 从几何数据中识别主视图、俯视图、侧视图等工程视图结构。 |
| 视图判定载荷 | 视图语义校正阶段专用的精简输入协议；由 `task`、`file_hint`、`local_rule_summary`、`layout_summary`、`candidate_views`、`projection_evidence`、`dimension_summary` 和 `output_contract` 组成，用于让大模型校正视图结构。文件名提示只作为弱证据，不得覆盖几何布局证据；尺寸摘要保留轻量标注列表以表达标注分布。它不包含视频策略、完整 JSON Schema、全量原始实体、实体样本、完整视图实体列表或后续建模脚本约束。 |
| 图纸证据包 | 语义理解阶段使用的可追溯证据集合；包含候选视图、候选尺寸、候选几何特征、空间关系、来源图元和不确定项，但不提前固化最终工程语义。 |
| 尺寸来源裁决器 | 语义策略内部判断尺寸来源的确定性角色；负责根据重建上下文区分标注尺寸与几何尺寸，并产出初始假设。它不绑定尺寸语义、不升级候选尺寸，也不生成追问。 |
| 语义裁决 | 基于图纸证据包判断视图角色、尺寸角色、特征角色、派生尺寸和追问需求的步骤；它的输出必须可追溯到证据包。 |
| 语义裁决会话 | 语义重建内核中管理一次语义裁决状态转换的角色；负责调用大模型语义裁决、应用语义裁决阶段的澄清答案、替换或保留本地策略追问、标记追问来源并触发阶段确认。它不生成零件语义、不选择建模路径，也不执行建模指令。 |
| 语义策略假设装配器 | 语义策略内部维护用户可见 assumptions 的角色；负责追加用户补充提示、局部恢复解除阻塞和裸线性尺寸未绑定等说明。它不改变尺寸绑定结果、不生成新候选，也不解释图纸语义。 |
| 语义尺寸权限视图 | 第二阶段语义输出校验使用的只读权限解释角色；集中说明 `key_dimensions` 可使用哪些已裁决尺寸、哪些构造尺寸只能保留局部语义、哪些值仍属于未授权。语义裁决成功后，它优先使用 `semantic_adjudication`，旧 `dimension_plan` 只作为裁决失败时的兼容兜底。 |
| 尺寸权限池 | 语义裁决后按使用权限划分的尺寸集合，包括 `allowed_dimensions`、`construction_dimensions`、`candidate_dimensions` 和 `unresolved_dimensions`。下游建模只能直接使用已裁决或用户确认的尺寸权限池；候选池只提供证据和追问来源。 |
| 尺寸权限计划 | 由尺寸绑定生成尺寸权限池的结构化计划；负责把尺寸分入建模许可、构造、候选、未决和排除集合，并集中维护这些集合的使用规则。 |
| 尺寸绑定构建器 | 将原始标注尺寸转换为语义绑定的角色；负责文本尺寸分类、线性标注 span 计算、几何近邻候选和尺寸链候选生成。它只产生已明确标注语义或 `binding_status="candidate"` 的候选绑定，不负责把候选升级为建模许可。 |
| 候选尺寸 | 本地尺寸链、投影关系、相邻尺寸组合、重复标注或几何近邻推断得到的可能尺寸角色；它必须带有 `binding_status="candidate"`，只能进入 `candidate_dimensions`，不等同于建模许可。 |
| 建模许可尺寸 | 已经由语义裁决或用户澄清确认、可被建模任务和建模指令直接使用的尺寸；通常位于 `allowed_dimensions`，或作为带明确角色和上下文的 `construction_dimensions` 使用。 |
| 构造尺寸 | 已由语义裁决确认可用于建模构造步骤的尺寸；它可能是局部分段、局部特征尺寸或重复特征尺寸，但不一定能单独命名为总长、深度、孔径、对边等主体关键尺寸。它与 `allowed_dimensions` 分属不同权限池：`allowed_dimensions` 可直接表达主体关键尺寸，构造尺寸必须带着具体角色和特征上下文使用。 |
| LLM 调用阶段 | 系统与大模型交互的可命名阶段，例如视图语义校正、图纸语义裁决、语义生成和建模指令生成；它用于说明一次大模型调用承担的职责边界。 |
| DeepSeek 公共调用选项 | 所有 DeepSeek 调用共享的配置解释层；负责注入 `user_id`、请求超时、阶段化 thinking 配置和官方 reasoning effort 取值校正。默认所有阶段统一使用 `deepseek-v4-pro`，但仍保持独立阶段请求和结构化载荷传递。它不改变阶段载荷内容，只统一 API 调用参数。 |
| GUI 运行配置适配器 | GUI 保存格式到运行时配置的转换角色；负责把设置窗口中的缓存目录、缓存 TTL、DeepSeek 模型、超时、阶段 thinking 和推理强度偏好转换为处理管道可消费的结构。GUI 表单不直接解释 DeepSeek API 细节或缓存兼容字段。 |
| LLM 输入载荷 | 发送给大模型的阶段化结构化输入；它不同于本地缓存、输出产物或遥测记录。全量原始实体只供本地分析层使用，不直接作为 LLM 输入；大模型调用应分别接收视图判定载荷、语义理解载荷或建模任务载荷。 |
| 语义生成 | LLM 分析图纸后输出零件类型、特征列表和置信度评估。 |
| 零件语义后处理器 | 第二阶段 LLM 返回结构化零件语义后的规范化角色；在 schema 校验和建模任务装配前执行，负责规范化建模路径、降级不完整回转语义、移除未授权 `key_dimensions`，并把英文用户可见风险替换为中文兜底提示。它不新增图纸语义，也不把候选尺寸升级为建模许可。 |
| 语义理解载荷 | 语义生成阶段专用的精简输入协议；用于让大模型把图纸证据包中的候选视图、候选尺寸、投影关系和关键几何证据解释为同一个三维零件及其可建模特征。它可以包含关键轮廓、轴线、圆/弧/孔、尺寸候选和不确定项等证据摘要，但不包含全量原始实体、完整视图实体列表、大量 entity pair 明细或建模脚本约束。 |
| 建模任务载荷 | 建模指令生成阶段专用的精简输入协议；由 `object`、`features`、`dimensions`、`constraints` 和 `recovery_hints` 组成，用于告诉大模型要建模的物件、已有特征、可用尺寸、执行约束和恢复提示。它不包含原始几何实体、视图实体列表、本地关系明细、本地展开的操作步骤或 `features.key_dimensions`；所有可用于建模的尺寸只能来自 `dimensions` 节，具体建模操作顺序仍由建模指令生成阶段的大模型产出。 |
| 建模任务构建器 | 将零件语义和语义裁决结果转换为建模任务载荷的合同转换角色；它只做结构化裁剪和任务装配，不重新理解图纸、不按具体零件类型写特殊规则，也不从缺失字段中推断新语义。它会在装配前再次经过零件语义后处理器，防止未授权尺寸或英文风险绕过第二阶段出口。零件语义是它的输入，不再作为建模指令生成阶段大模型的直接输入。 |
| 建模任务完整性诊断器 | 建模任务载荷进入建模指令生成阶段前的诊断角色；负责判断主体来源、路径合同、权威尺寸和主体闭合风险是否足以生成可靠主体脚本。它不重新理解图纸、不生成脚本，也不把候选或未决尺寸升级为建模许可。 |
| 建模任务出口 | 语义重建内核中进入建模指令生成阶段前的唯一载荷出口；负责调用建模任务构建器生成建模任务载荷，并把该载荷交给建模指令生成代理。它不重新理解图纸、不选择建模路径，也不执行 FreeCAD 脚本。 |
| 建模任务尺寸构建器 | 建模任务构建器内部用于生成 `dimensions` 载荷的角色；语义裁决成功时只输出已裁决建模尺寸，语义裁决缺失或失败时才保留兼容尺寸权限池。 |
| 建模任务上下文构建器 | 建模任务构建器内部用于生成 `constraints` 和 `recovery_hints` 载荷的角色；集中维护部分建模策略、禁止输入、建模路径裁决摘要和用户澄清提示优先级。 |
| 平面建模语义 | 零件语义中的一种受约束子类型；用于描述可平面拉伸件所需的外轮廓、减料特征、拉伸厚度及其他可由平面拉伸路径稳定执行的信息。 |
| 回转建模语义 | 零件语义中的一种受约束子类型；用于描述回转体路径所需的轴线、闭合母线点列、旋转角度和不确定性。该字段不适用时应显式为 `null`。 |
| 语义闭合 | 建模路径所需关键字段均已确定且可执行的状态；由程序依据结构化语义契约做最终裁决。若关键字段缺失或不确定，应进入追问，而不是改走更自由的建模路径继续猜测。 |
| 路径契约 | 某条专用建模路径自己的准入条件集合，用于定义该路径需要哪些显式建模语义、如何判定语义闭合以及何时拒绝执行；不得从旧兼容字段反推出缺失语义。 |
| 路径注册表 | 专用建模路径在裁决侧的集中登记表，记录路径 ID、展示标签、路径契约评估器、路径层追问构造器和路由建模结果构造器。 |
| 建模路径裁决视图 | 对 `modeling_path_decision` 字典的只读语义视图，用于集中解释建模路径、候选路径、路径层追问、缺失契约字段和降级上下文；外部结果仍可保持 dict 兼容，但新代码不应在调用点重复解析底层字段组合。 |
| 路径优选 | 当多条建模路径都满足各自路径契约时，由大模型在合法候选集合中选择更贴合图纸建模意图的路径；若仅有一条路径合法，则由程序直接选择。 |
| 路径层追问 | 语义生成之后、建模执行之前的局部恢复问题；用于补齐拉伸深度、拉伸方向或路径优选等执行合同字段，用户回答后不重新触发视图分析或零件语义生成。 |
| 路径层追问答案应用器 | 将路径层追问答案写回零件语义副本的角色；负责补入拉伸深度、拉伸方向、路径优选和用户补充建模提示。它不装配 pending 返回结果、不重新选择建模路径，也不解释图纸语义。 |
| 澄清后建模结果装配器 | 用户澄清后决定建模结果走向的角色；负责处理语义置信度阻塞、路径契约继续追问、用户提示触发的语义重建兜底，以及调用建模结果生成函数。它不应用用户答案、不重新生成零件语义，也不执行 FreeCAD 脚本。 |
| 建模指令 | LLM 生成的 FreeCAD Python 脚本及其配套说明。 |
| 建模指令后处理器 | 第三阶段 LLM 返回建模指令 JSON 后、AI 脚本执行前的规范化角色；负责移除未获语义裁决许可的 `key_dimensions`，中文化 warnings/instructions/skipped_features 等用户可见元数据。它不改写 FreeCAD 脚本，不补充新尺寸，也不把 warning 或 skipped detail 升级为建模事实。 |
| 建模约束 | 建模指令生成和 AI 脚本执行共同遵守的 FreeCAD API 白名单、黑名单、特征守门、局部容错和重试纪律；主体建模应优先完成并保存，孔、倒角、圆角、槽、螺纹、局部切除等细节特征应逐项尝试，失败时记录为跳过细节而不是导致整次建模失败。 |
| 建模脚本可执行性校验器 | AI 脚本通过建模约束白名单后、进入 FreeCAD 执行前的第二道诊断角色；负责拦截缺少 `final_shape`、缺少 `Part.show(final_shape, ...)`、缺少文档重算、未检查闭合轮廓或线框边未转换等明显会运行失败的脚本形态。它不理解图纸语义、不修复脚本，也不替代 FreeCAD 的真实执行。 |
| 脚本质量恢复 | AI 脚本在执行前未通过可执行性校验时进入的待恢复状态；系统保存校验错误、上一版建模指令、失败脚本和当前图纸上下文，等待用户补充建模意图后重新生成脚本。它不同于 FreeCAD 运行时崩溃，也不代表图纸解析或语义裁决已经失败。 |
| 局部恢复 | 语义失败后仅重跑语义生成和指令生成，复用之前已成功的解析/视图/尺寸结果。 |

## 管线状态

| 术语 | 含义 |
|------|------|
| `completed` | 处理正常完成，模型已生成。 |
| `partial_completed` | 处理生成了部分建模成果；模型主体可打开、可检查、可导出，但部分细节被跳过并记录缺失内容和风险。 |
| `failed` | 不可恢复的失败，需要人工介入或更换输入。 |
| `needs_clarification` | 语义置信度不足，等待用户澄清后可继续。 |
| 用户停止处理 | 用户在阶段确认点主动不放行后结束当前单图纸处理；这不是图纸、AI 或 FreeCAD 失败。 |

## 交互机制

| 术语 | 含义 |
|------|------|
| 追问清单 | 语义不足时系统向用户提出的结构化问题列表，每项含 id、text、kind、options 等字段；每次追问都应允许用户可选补充自然语言提示，用于说明图纸意图或建模偏好。 |
| 最小人工介入 | 智能处理的交互原则；系统应优先通过阶段内模型自纠、本地校验和带风险记录继续完成处理，用户主要承担进度监督、错误发现、终止、重试或触发自纠的角色，而不是常规语义补全者。只有系统越过安全边界或用户主动指出阶段结果错误时，才进入候选确认或待恢复。 |
| 模型自纠回合 | 同一 LLM 调用阶段内，本地校验发现输出不满足合同后触发的受控修正过程；系统把校验错误、证据 ID、候选解释和输出合同反馈给大模型，让模型重新裁决或重新生成。它不等同于跨阶段长对话，也不允许把全量原始实体重新塞回模型。 |
| 模型自纠状态 | 模型自纠回合进入、进行和结束时必须向用户可见的运行状态；GUI、日志、阶段报告和大模型调用记录都应展示所属阶段、当前轮次、最大轮次、触发原因、主要校验问题、修正目标和本轮结果。它不能作为后台静默重试隐藏起来。 |
| 阶段内自纠会话 | 组织单个 LLM 调用阶段内模型自纠回合的领域角色；它接收本地校验问题、阶段证据包、候选解释和输出合同，控制自纠轮次、构造给大模型的修正请求，并输出修正后的阶段结果、用户候选确认或失败报告。GUI 只展示状态和候选项，校验器只产出问题，阶段生成器只负责生成或重生阶段结果。 |
| 自纠请求 | 阶段内自纠会话发送给当前阶段大模型调用的结构化修正输入；包含所属阶段、当前轮次、上一轮输出摘要、本地校验问题、输出合同、可追溯证据引用和修正目标。它不是新的全量图纸输入，也不应绕过阶段化载荷协议。 |
| 自纠结果 | 阶段内自纠会话返回给管线的结构化结果；包含状态、修正后的阶段输出、自纠日志、风险说明、候选选项和下一步动作。它用于决定继续处理、带风险继续、进入用户候选确认、进入待恢复任务或明确失败。 |
| 自纠停止规则 | 阶段内自纠会话的轮次上限和出口约束；每个阶段最多执行 2 轮模型自纠。第 1 轮用于修正明显合同错误，第 2 轮用于修正仍未闭合的局部不一致。达到上限后不得继续后台重试，必须转为用户候选确认、待恢复任务或明确失败报告，并记录停止原因。 |
| 自纠放行规则 | 模型自纠结果是否可自动继续处理的判断规则；JSON 合法性、字段缺失、脚本 API 用错、未授权输出字段、缺少 `final_shape` 等合同类错误，修正并通过本地校验后可自动继续。工程语义类分歧应优先由模型给出解释、依据和风险，并在阶段报告中显式记录后继续推进；只有影响主体外形、主要体量、关键尺寸来源、建模路径安全边界，且模型自纠后仍无法形成可审计判断时，才转为用户候选确认或待恢复任务。 |
| 用户候选确认 | 模型自纠后仍无法在安全边界内形成可审计判断，或用户在阶段确认中指出阶段结果错误时展示给用户的选择机制；界面应优先给出可选解释、推荐项、依据和风险，让用户确认、排除、触发重试或选择“不确定”，同时保留可选补充说明输入框。它不是常规流程中的必经人工补全步骤。 |
| 追问出口 | 语义重建内核中装配待澄清返回结果的角色；负责把追问清单、追问阶段、待恢复上下文、待定零件语义和待定建模结果组合成统一结构。它不决定图纸语义、不生成候选尺寸，也不执行用户答案。 |
| 候选确认追问 | 针对 `candidate_dimensions` 生成的单选追问；它负责展示候选值、候选来源和“不确定”选项。用户可以确认某个候选值，使其升级为 `user_confirmed` 的已裁决尺寸，也可以选择“不确定 / 暂不使用这些候选”，使其从自动绑定中排除。 |
| 追问答案 | 用户对追问清单的扁平键值回答，key 对应问题的 id；可同时包含可选自然语言补充，补充内容不替代结构化答案，但可作为局部恢复的额外上下文。 |
| 语义澄清答案应用器 | 将结构化追问答案应用到尺寸绑定副本的角色；负责用户确认候选、排除候选、绑定主体轴尺寸和提升细节特征尺寸，不重新生成追问、不重新理解图纸。 |
| 补充建模提示 | 用户在追问时可选填写的自然语言说明，用于表达图纸意图、建模偏好或对具体问题的解释；它参与局部恢复，但不能直接覆盖已解析出的图纸事实和主体级硬约束。 |
| 用户语义覆盖 | 用户自然语言补充明确覆盖大模型原有语义判断的情况；用户补充可优先修正建模意图和细节解释，但不得直接覆盖已解析出的图纸事实、关键尺寸来源、主体方向或主体外形，发生冲突时必须记录覆盖项、原因和风险，必要时继续澄清。 |
| 保底追问 | 系统无法在安全边界内继续自动处理时才触发的追问机制；它不是常规建模步骤，只有主体外形、主要体量、方向、关键尺寸来源或建模路径执行合同无法可靠裁决时才应阻塞处理。 |
| 澄清上下文 | 保存第一次分析中间产物的对象，用于局部恢复时避免重新调用 LLM。 |
| 待恢复任务存储 | 保存待恢复任务现场的持久化机制，使用户可在之后继续补充追问答案；它不是分析缓存，也不代表智能分析已经完成。恢复成功后，该文件对应的旧分析缓存必须被主动失效，防止后续首次处理命中不含用户澄清信息的过时缓存。 |
| 澄清会话 ID | 随机 uuid4，前缀 `clar_`，唯一标识一次未完成的交互循环。 |
| 阶段确认 | 交互式单图纸智能处理中，在大模型阶段完成且自动自纠执行完毕后向用户汇报阶段结论、自纠记录、证据和风险的监督点；用户可选择继续、停止或带部分成果重跑。自动自纠在阶段确认之前触发，校验失败时自动进入自纠回合，最多 2 轮；自纠结果影响阶段确认的呈现状态和可用动作。它不应把普通工程语义补全变成常规人工输入步骤。 |
| 监督动作 | 用户在阶段确认点可选择的处理动作；集合为继续、停止和带部分成果重跑。继续在主体安全边界未闭合时不可用；重跑弹出勾选界面让用户选择保留的部分成果后触发。监督动作表达用户对阶段结果的监督意图，不直接替代结构化语义裁决。 |
| 带部分成果重跑 | 用户在阶段确认点选择重跑当前阶段时，通过勾选界面选择保留已认可的部分成果，系统将勾选项作为约束传入 LLM 重新生成未认可部分；勾选粒度为特征级（视图角色、主体特征、增材特征、减材特征、关键尺寸），不暴露坐标系、尺寸来源等内部字段。 |
| 多视图工程图 | 同一零件的两个或更多彼此独立的正交投影视图；这些视图在图纸上是分开的图形对象，并可通过长对正、高平齐、宽相等等投影关系互相验证。 |
| 单一轮廓图 | 围绕一个零件外形或截面展开的单张平面图；它可以包含孔、圆弧、中心线、尺寸线和局部空隙，但这些内容仍服务于同一个连续几何对象，不因版面上有间隔就自动拆成多个视图。系统应优先以连续几何对象作为其正证据。 |
| 智能处理主入口 | 面向用户承接所有图纸处理；先完成图纸理解，再在平面拉伸路径与语义重建路径之间做选择。 |
| 视图初判 | 本地规则对图纸视图结构形成的候选判断和证据整理；它服务于后续视图语义校正，不是智能模式下的最终视图裁决。 |
| 视图语义校正 | 基于大语言模型对本地初判、几何摘要和尺寸摘要进行的主要视图判断；在智能模式下，它承担视图语义裁决职责。 |
| 可平面拉伸图 | 适合用单一平面轮廓沿一个明确方向直接拉伸建模的图纸；它通常是单一轮廓图，但不等同于所有单视图图纸。 |
| 建模家族 | 具有稳定几何生成规律的一类零件表达方式，例如平面拉伸件、回转体或其他可由专用建模路径承接的类别。 |
| 建模路径 | 系统最终采用的建模方式，例如平面拉伸路径或语义重建路径；它回答“最后怎么建”，不再对应用户可见模式。建模路径由程序依据结构化语义裁决，而不是直接接受大模型的自由文本判断。 |

## 基础数据

| 术语 | 含义 |
|------|------|
| `geometry_data` | CAD 解析产出的标准字典结构，包含 `version`、`units`、`entities`。 |
| 闭合轮廓 | 几何实体首尾相连形成的封闭多边形，是基础拉伸的输入单位。 |
| 实体 | DXF 中的 LINE、CIRCLE、ARC、LWPOLYLINE、TEXT、MTEXT、DIMENSION 等图形元素。 |

## 系统角色

| 术语 | 含义 |
|------|------|
| 分析缓存 | `.cache/analysis` 下的智能分析结果缓存，按文件+参数幂等；只缓存完整的、可直接继续执行的分析结果，不含未决澄清问题的半成品。 |
| 预览缓存 | 图纸预览图的可复用缓存；它只代表预览图可复用，不代表 CAD 解析结果或智能分析结果可复用。 |
| LLM 遥测 | LLM 调用阶段的本地观测记录，用于查看耗时、token、请求、响应、DeepSeek 上下文缓存命中 token 和推理 token；它不是发送给大模型的新输入，也不是分析缓存。 |
| FreeCAD 桥接 | 检测 direct/subprocess 模式，在 FreeCAD Python 中执行建模脚本。 |
| FreeCAD 增强包 | 可选放入项目目录的 FreeCAD 便携运行时，用于让系统优先使用项目级 `tools/freecad/.../bin/python.exe` 执行自动建模；它不等同于仓库默认提交 FreeCAD 本体，也不替代用户通过 `FREECAD_BIN_PATH` 指向系统安装。 |
| AI 脚本运行器 | 执行 LLM 生成的 FreeCAD 脚本并收集 STEP/FCStd 产物。 |
