# 统一智能处理入口

系统不再把“是否使用大模型”暴露成用户可选模式；GUI 只保留统一的智能处理入口，所有图纸都先由大模型参与图纸理解，再由程序依据结构化语义和路径契约选择后续建模路径。平面拉伸路径是第一条专用建模路径，后续还可扩展回转体路径等稳定建模家族；语义重建路径不再作为默认路径，而是保留给无法归入专用路径的复杂图纸作为兜底。这样可以保留平面图纸的低复杂度建模优化，同时避免把“无需复杂三维重建”误设计成“无需大模型参与理解图纸”。迁移期允许内部代码暂时保留 `basic` / `intelligent` 兼容命名，但这些命名不再定义产品语义，应随后续重构逐步收敛。
