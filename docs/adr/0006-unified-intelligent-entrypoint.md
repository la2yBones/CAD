# 统一智能处理入口

系统不再把“是否使用大模型”暴露成用户可选模式；GUI 只保留统一的智能处理入口，所有图纸都先由大模型参与图纸理解，再由系统在平面拉伸路径与语义重建路径之间选择后续执行方式。这样可以保留平面图纸的低复杂度建模优化，同时避免把“无需复杂三维重建”误设计成“无需大模型参与理解图纸”。迁移期允许内部代码暂时保留 `basic` / `intelligent` 兼容命名，但这些命名不再定义产品语义，应随后续重构逐步收敛。
